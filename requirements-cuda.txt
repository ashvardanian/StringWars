--extra-index-url https://pypi.nvidia.com

# RAPIDS cuDF for CUDA 12
# https://github.com/rapidsai/cudf
cudf-cu12==26.*; platform_system == "Linux" and (platform_machine == "x86_64" or platform_machine == "aarch64") and python_version >= "3.10" and python_version < "3.14"

# StringZillas GPU build — v5 wheels are not yet on PyPI; build from the neighboring source:
#
#     uv pip install ../StringZilla/python/stringzillas-cuda
#
# https://github.com/ashvardanian/stringzilla
